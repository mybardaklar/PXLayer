include ./settings
include ../Components/_init
include ../Widgets/_init
include ../Partials/_init

block PageSettings

doctype html
html(
  lang=Settings.lang,
  dir=Settings.dir,
  class=PageSettings.pxlMainLayer ? 'has-pxl-main-layer' : null
)&attributes(PageSettings.htmlAttr)

  //- Head
  head
    block Head
      include ./head
  
  //- Body
  body(class=PageSettings.pxlMainLayer ? 'has-pxl-main-layer' : null)&attributes(PageSettings.bodyAttr)
    block Layers
    |
    |

    // #pxl-body 
    section.pxl-main-layer
      block Body
    // /#pxl-body 

    |
    |
    //- Scripts
    // Scripts 
    block Scripts
      include ./scripts
