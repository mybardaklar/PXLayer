-
  const Settings = {
    lang: 'en',
    dir : 'ltr'
  }

  const PageSettings = {
    themeColor  : '#2B32B2',
    title       : 'PXLayer Page'
  }

  const OpenGraph   = {}
  const TwitterCard = {}
