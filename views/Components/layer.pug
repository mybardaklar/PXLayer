mixin Layer(attr)
  // section.pxl-layer 
  //- layer state off
  input.pxl-layer__state-off(
    type="radio",
    id=attr.name + "__state-off",
    name=attr.name,
    checked
  )

  section.pxl-layer(class=attr.class)
    //- layer state on
    input.pxl-layer__state-on(
      type="radio",
      id=attr.name + "__state-on",
      name=attr.name,
      checked=attr.checked
    )

    //- layer container
    .pxl-layer__container
      .pxl-layer__wrapper
        //- layer background
        label.pxl-layer__background(for=attr.name+"__state-off")

        //- layer body
        .pxl-layer__body(
          pxl-innerPosition=attr.innerPosition,
          pxl-innerAnimation=attr.innerAnimation
        )
          //- layer inner
          .pxl-layer__inner
            block
  // ## section.pxl-layer 
