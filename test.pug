div(
  pxl-wrap="in: flex",
  pxl-column="md: 3",
  pxl-justify-content="in: between, md: center"
)
  .pxl-column


div(
  pxl:lg="grid(flex) grid-columns(5) justify(center)",
  pxl:md="grid-columns(3)"
)
  div(pxl="p-x(5)")
