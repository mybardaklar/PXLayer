mixin dropdown(type = 'hover')
  .pxl-dropdown(class="pxl-dropdown--" + type)
    block

mixin dropdown__trigger
  .pxl-dropdown__trigger(tabindex="0")
    block

mixin dropdown__body
  .pxl-dropdown__body
    block